# Rebuilding figures

Run the figure scripts from the package root after installing `requirements.txt`. The archived source-data workbooks and CSVs contain derived public-safe records only. Raw provider data are not redistributed.
